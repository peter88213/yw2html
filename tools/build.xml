<?xml version="1.0" encoding="UTF-8"?>
<project name="yw2html" basedir=".">
	<property name="release" value="1.6.1" />
	<property name="src" location="../src" />
	<property name="test" location="../test" />
	<property name="build" location="../build" />
	<property name="dist" location="../dist" />
	<property name="docs" location="../docs" />
	<property name="project-name" value="${ant.project.name}" />
	<property name="release-name" value="${project-name}_v${release}" />

	<target name="build" description="inline all library modules">
		<delete dir="${build}" />
		<delete dir="${dist}" />
		<delete file="${test}/yw2html.py" />
		<exec executable="python" failonerror="true">
		    <arg value="${src}/build_yw2html.py"/>
		</exec>
		<fixcrlf encoding="utf-8" eol="lf" srcdir="${test}" includes="**/*.py" />
	</target>

	<target name="test" description="inline all library modules">
		<exec executable="python" failonerror="true">
		    <arg value="${test}/test_yw2html.py"/>
		</exec>
	</target>

	<target name="dist" description="generate the distribution">
		
		<mkdir dir="${build}" />
		<mkdir dir="${dist}" />
		<mkdir dir="${build}/yw2html_v${release}" />

		<copy file="${test}/yw2html.py" todir="${build}/yw2html_v${release}" />
		<replace encoding="utf-8" file="${build}/yw2html_v${release}/yw2html.py" token="@release" value="${release}" />

		<copy todir="${build}/${release-name}/examples"> 
			<fileset dir="../bat" />
		</copy>

		<copy todir="${build}/${release-name}/examples/template"> 
			<fileset dir="../template" />
		</copy>

		<copy file="../LICENSE" todir="${build}/yw2html_v${release}" />
		<copy file="${docs}/usage.md" tofile="${build}/yw2html_v${release}/README.md" />

		<zip destfile="${dist}/yw2html_v${release}.zip" basedir="${build}/yw2html_v${release}" />

		<copy file="${docs}/template/index.md" overwrite="True" todir="${docs}" />
		<replace encoding="utf-8" file="${docs}/index.md" token="0.99.0" value="${release}" />

	</target>

	<target name="clean" description="clean up">
		
		<delete file="${test}/yw2html.py" />
		
	</target>

</project>